package seleniumWeb;

/*
WebDriver is an interface which has a common behavior and we up cast 
so that the same behavior can be used across the Classes.Example

Interface: Consider WebDriver has the behavior of Switch*/

public interface WebDriver

{
   public int A = 90;
	void on();

	void off();
 
	int voltage = 220;;
}

